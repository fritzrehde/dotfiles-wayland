watched-command = "wifi.sh list"
interval = 1.0
field-separator = ":"
header-lines = 1

[keybindings]
"l" = [ "exec -- wifi.sh connect", "exit" ]
"enter" = [ "exec -- wifi.sh connect", "exit" ]
"h" = [ "exec -- wifi.sh disconnect", "reload" ]
